import { Button } from "react-bootstrap";

const Administrador = () => {
    return (
        <section>
            {/* Aqui va el main de la pagina admin  */}
            <h1>Productos disponibles</h1>
            <Button variant="primary"></Button>
        </section>
    );
};

export default Administrador;